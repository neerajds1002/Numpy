import numpy as np
data = np.array([[1, 2], [3, 4], [5, 6]])

#shape of the array(A tuple that contains the number of elements (i.e., the length) for each dimension (axis) of the array.)
data.shape

#size of the array(The total number elements in the array.)
data.size

#Number of dimensions (axes).
data.ndim

#Number of bytes used to store the data.
data.nbytes

#The data type of the elements in the array.
data.dtype
